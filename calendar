import calendar
a=int(input("Enter any year: "))
b=int(input("Enter any month: "))
res=calendar.month(a,b)
print(res)

------prime number ------
for i in range(100,999):
    c=0
    for j in range(2,i):
        if i%j==0:
         c=c+1
    if c==0 :  
     print(i)
